type User {
  id: ID!
  email: String!
  createdAt: String!
  updatedAt: String!
}

input UserInput {
  email: String!
  password: String!
}

type Query {
  me: User
}

type Mutation {
  signUp(input: UserInput!): User!
  signIn(input: UserInput!): User!
}
